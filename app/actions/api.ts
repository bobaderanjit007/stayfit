export const serverapi = "http://localhost:7000/v1";

export const membersapi = `${serverapi}/member`;

export const staffapi = `${serverapi}/staff`;

export const enquiryapi = `${serverapi}/enquiry`;
